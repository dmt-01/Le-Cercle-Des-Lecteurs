@startuml
start

:L'utilisateur accède à la page inscription;
:Remplit le formulaire\n(username, email, password);

if (Formulaire valide ?) then (non)
  :Affiche les erreurs\nde validation;
  stop
else (oui)
 :Envoi de la requête au backend;

 if (Email déjà existant ?) then (oui)

  :Affiche "Email déjà utilisé";
  stop

 else (non)

  :Hash du mot de passe (Bcrypt);
  :Création du compte en BDD;
  :Sélection des genres préférés;
  :Sauvegarde des préférences;
  :Génération du token JWT;
  :Redirection vers l'accueil;
  stop

 endif
endif

@enduml
